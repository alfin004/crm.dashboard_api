# FastAPI CRUD Project

This archive contains the project scaffold.

Structure:
app/
  api/
  core/
  models/
  schemas/
  repositories/
  services/
  dependencies/
  utils/
  main.py

Run:
uvicorn app.main:app --reload
